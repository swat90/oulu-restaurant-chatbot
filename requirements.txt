# Core
streamlit
python-dotenv

# Supabase
supabase

# LangChain — latest compatible set
langchain
langchain-core
langchain-community

# LLM — Gemini primary
langchain-google-genai
google-generativeai

# LLM — OpenAI fallback
langchain-openai

# Embeddings & RAG
sentence-transformers

# Email
resend

# Testing (only needed locally, not on Streamlit Cloud)
pytest
pytest-mock
